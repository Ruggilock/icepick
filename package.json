{
  "name": "icepick",
  "version": "1.0.0",
  "description": "Multi-Chain DeFi Liquidation Bot (Base + Arbitrum)",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "test": "bun test",
    "test:connection": "bun run scripts/test-connection.ts",
    "verify": "bun run scripts/verify-setup.ts",
    "generate-wallet": "bun run scripts/generate-wallet.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4",
    "ioredis": "^5.8.2",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
